# Mux API Credentials
# Get these from https://dashboard.mux.com/settings/access-tokens
MUX_TOKEN_ID=your-mux-token-id
MUX_TOKEN_SECRET=your-mux-token-secret

# Mux Signing Keys (required for signed/private assets)
# Get these from https://dashboard.mux.com/settings/signing-keys
# The private key should be base64 encoded
MUX_SIGNING_KEY=your-signing-key-id
MUX_PRIVATE_KEY=your-base64-encoded-private-key

# Optional: Host port to bind (default: 3000)
PORT=3000

# Optional: PostgreSQL password (default: simulive)
# Change this in production!
POSTGRES_PASSWORD=simulive

# Optional: Admin password for protecting the admin interface
# If not set, admin is open (only use in development!)
ADMIN_PASSWORD=

# Optional: Set to "true" if using HTTPS (enables secure cookies)
# Leave empty or "false" for HTTP deployments
SECURE_COOKIES=
